import React from "react";
import ReactDOM from "react-dom";

import App from "./App";

// Basic boilerplate code to inject everything into the root div in public/index.html.
ReactDOM.render(<App />, document.querySelector("#root"));
